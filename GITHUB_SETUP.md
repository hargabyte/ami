# GitHub Repository Setup

The AMI project has been initialized as a private repository on GitHub.

- **URL**: https://github.com/hargabyte/ami
- **Visibility**: Private
- **Main Branch**: `main`

## Commands Used:
```bash
git init
git add .
git commit -m "Initial commit v0.1.0"
gh repo create ami --private --source=. --remote=origin --push
```
